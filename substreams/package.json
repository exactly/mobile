{
  "name": "@exactly/substreams",
  "scripts": {
    "test": "concurrently 'npm:test:*' --group -c auto",
    "test:fmt": "cargo fmt --check",
    "test:clippy": "cargo clippy -- --deny warnings",
    "test:proto": "protolint *.proto"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.36.0",
    "concurrently": "^8.2.2",
    "protolint": "^0.50.5"
  }
}
