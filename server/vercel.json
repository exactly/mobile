{
  "buildCommand": "if [ -z \"${__VERCEL_DEV_RUNNING+1}\" ]; then bun run build:app; fi",
  "outputDirectory": "/",
  "trailingSlash": false,
  "cleanUrls": true,
  "rewrites": [{ "source": "/:path((?!api/).*)", "destination": "/app/:path*" }],
  "$schema": "https://openapi.vercel.sh/vercel.json"
}
